<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Hostels</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #19585D;
            color: #FFFFFF;
        }
        h1 {
            color: #F3FE39;
            text-align: center;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            background-color: #FFFFFF;
            color: #19585D;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
        }
        a {
            text-decoration: none;
            color: #F3FE39;
            font-weight: bold;
            transition: color 0.3s ease;
        }
        a:hover {
            color: #2FFFCD;
        }
        ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }
        li {
            background-color: #e7e7e7;
            margin: 10px 0;
            padding: 15px;
            border-radius: 8px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: background-color 0.3s;
        }
        li:hover {
            background-color: #d4d4d4;
        }
        button {
            background-color: #dc3545;
            color: white;
            border: none;
            border-radius: 4px;
            padding: 8px 16px;
            cursor: pointer;
            transition: background-color 0.3s;
            font-weight: bold;
        }
        button:hover {
            background-color: #c82333;
        }
        .actions {
            display: flex;
            gap: 10px;
        }
        .no-hostels {
            text-align: center;
            color: #FFFFFF;
            font-size: 1.2em;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Hostels</h1>
        <div style="text-align: right; margin-bottom: 20px;">
            <a href="/admin/hostels/new">Add New Hostel</a>
        </div>

        <% if (hostels.length > 0) { %>
            <ul>
                <% hostels.forEach(hostel => { %>
                    <li>
                        <div>
                            <strong><%= hostel.name %></strong> - <%= hostel.location %>
                        </div>
                        <div class="actions">
                            <a href="/admin/hostels/edit/<%= hostel._id %>">Edit</a>
                            <form action="/admin/hostels/delete/<%= hostel._id %>" method="POST" style="display: inline;">
                                <button type="submit">Delete</button>
                            </form>
                        </div>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p class="no-hostels">No hostels available.</p>
        <% } %>
    </div>
</body>
</html>
